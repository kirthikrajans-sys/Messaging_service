  ## Running teh Back-end code

  Run `node server.js` to run the back end server 
  forward the port to public and configure it into twilio

  
  ## Running the front-end code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

## Twilio Configuration 
  configure the backend server for inbound and outbound message to twilio console 
